{% apply spaceless %}
    {% set dropzoneContentId = dropzoneContentId ?: 'page-content-dropzone-JPzHJO' %}

    <div class="content">
        <div data-bsi-dropzone="{{ dropzoneContentId }}" class="flex-grow-1 d-flex flex-column">
            {% block main %}
                
            {% endblock %}
        </div>
    </div>
{% endapply %}